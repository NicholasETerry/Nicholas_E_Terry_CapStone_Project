@model Nicholas_E_Terry_CapStone.Models.CleanArticle

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<h1>Index</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>*@
<!-- The sidebar -->
<div id="mySidenav" class="sidenav">
    <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
    <a asp-controller="Contributor" asp-action="Index">HOME</a>
    @*<a href="#">BREAKING NEWS!</a>
    <a href="#">ABOUT</a>*@
</div>
<!-- Use any element to open the sidenav -->
<button type="button" class="btn btn-outline-primary" onclick="openNav()">&#9776;</button>
<!-- Add all page content inside this div if you want the side nav to push page content to the right (not used if you only want the sidenav to sit on top of the page -->
<form asp-action="ArticleReview">
    <div id="main">
        <div class="container">
            <div class="navbar">
                <a  href="#">Article Review Points ( 25 points )</a>
            </div>
            <div class="jumbotron">
                <p class="lead">@Model.Lead_paragraph</p>
                <details>
                    <summary>Click To Read Article</summary>
                    <p @*class="lead"*@>@Model.Word_count</p>
                </details>
                <hr class="my-4">
            </div>
            <select asp-for="ArticleTags" multiple="multiple">
                @foreach (var item in (List<string>)ViewBag.TagLibrary)
                {
                    <option value="@item">@item</option>
                }
            </select>
            <br><br>
            <div class="form-group">
                <label for="suggestions">Article Suggestions</label>
                <textarea asp-for="Comment" class="form-control" id="suggestions" rows="3"></textarea>
            </div>
            <input id="articleId" type="hidden" asp-for="Id" />
        </div>
    </div>
    <div class="form-group">
        <input type="submit" value="Submit Review" class="btn btn-primary" />
    </div>
</form>

    @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>*@
